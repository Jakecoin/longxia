---
summary: "Runtime plugin management is disabled in Longxia"
title: "Plugins"
---

# Plugins

Longxia does not support runtime plugin installation or plugin management.

The upstream plugin system is kept only as an internal loading mechanism for preapproved bundled capabilities. End users should not install packages, archives, or third-party extensions into a running Longxia deployment.
